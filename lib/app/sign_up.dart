import 'package:flutter/material.dart';

class AppSignUp extends StatelessWidget {
  const AppSignUp({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('Please Sign up my app')),
      ),
    );
  }
}
